#!/usr/bin/env node
var debug = require('debug')('react-starter');
var app   = require('../app');

app.set('port', process.env.PORT || 8000);

var server = applisten(app.get('port'), function() {
  debug('Express server listening on port ' + server.address().port);
});
